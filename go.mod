// Package doclock implements an in-memory document versioning and edit-lock
// service.
//
// The module is intentionally dependency-free: it uses only the Go standard
// library so that it builds and tests fully offline.
module github.com/fddhuwenjie/hwj-go-0008

go 1.22
